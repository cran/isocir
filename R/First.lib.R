.First.lib<-function (libname, pkgname){
    ## echo output to screen
    cat("## ISOtonic Inference for CIRcular Data\n")
    cat("## Departamento de Estadistica e Investigacion Operativa. Universidad de Valladolid.\n")
    ## assuming you would need the packages combinat and circular.
    require(combinat, quietly=TRUE)
    require(circular, quietly=TRUE)
}
